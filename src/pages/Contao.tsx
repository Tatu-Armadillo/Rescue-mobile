import React from "react";
import { ScrollView, Text } from "react-native";

const Contato: React.FC = () => {
    return (
        <ScrollView>
            <Text>Vai apresentar os contatos estativo do dono</Text>
        </ScrollView>
    );
}

export default Contato;
